# คู่มือการอัปโหลดแอปพลิเคชันขึ้น GitHub และใช้งานแบบ Local-First (localStorage)
คู่มือนี้จะแนะนำขั้นตอนการนำระบบรายงานความปลอดภัยเรือท่องเที่ยว **Chao Phraya Safety Tracker** ขึ้นระบบ GitHub และเปิดใช้งานแบบเว็บบอร์ดเดี่ยว (Single Page App) บน **GitHub Pages** โดยข้อมูลทั้งหมดจะจัดเก็บและทำงานบนเครื่องผู้ใช้อย่างรวดเร็ว ปลอดภัย และออฟไลน์ 100% ผ่านระบบ `localStorage` ในเว็บเบราว์เซอร์ของโทรศัพท์มือถือและคอมพิวเตอร์

---

## 💡 ทำไมแอปพลิเคชันเวอร์ชันนี้จึงยอดเยี่ยม?
1. **ข้อมูลจัดเก็บภายในเครื่อง (Local-First 100%)**: ระบบถูกเขียนให้ตรวจพบสถานะการเปิดใช้งานโดยอัตโนมัติ หากเปิดจากหน้าเว็บปกติหรือ GitHub Pages ระบบจะใช้พื้นที่เก็บข้อมูลในเบราว์เซอร์ (`localStorage`) ทันทีโดยไม่ต้องล็อกอินหรือตั้งค่าคลาวด์ใดๆ
2. **ระบบไฟล์เดียวเสร็จสมบูรณ์ (Single File Bundle)**: ระบบบิวด์ด้วยเครื่องมือพิเศษให้ซอร์สโค้ด CSS, JS และภาพทั้งหมดรวมอยู่ภายในไฟล์เดี่ยว (`dist/index.html` ขนาดเพียง ~1.6MB) หมดปัญหาเรื่องพาธไฟล์เสีย หรือโหลดรูปไม่ขึ้นบน GitHub Pages
3. **ความปลอดภัยสูงและทำงานออฟไลน์ได้**: ข้อมูลประวัติการตรวจสอบเรือทุกลำจะไม่ถูกส่งออกนอกอุปกรณ์หากคุณไม่ได้เชื่อมต่อ ช่วยให้ทำงานในจุดอับสัญญาณกลางแม่น้ำเจ้าพระยาได้เป็นอย่างดี

---

## 🛠️ ขั้นตอนที่ 1: การบิวด์เพื่อรับไฟล์ระบบล่าสุด
ทุกครั้งที่บิวด์ด้วยระบบล่าสุด ระบบจะเตรียมไฟล์ที่สมบูรณ์ไว้ให้เรียบร้อยที่โฟลเดอร์ `dist/`:
- **`dist/index.html`**: ไฟล์ระบบหลักที่รวมไฟล์ทั้งหมดเข้าเป็นหนึ่งเดียว เหมาะอย่างยิ่งสำหรับการเปิดใช้งานบน **GitHub Pages** หรือรันแบบออฟไลน์

---

## 🚀 ขั้นตอนที่ 2: วิธีนำขึ้น GitHub และเปิดใช้งาน GitHub Pages

### วิธีที่ง่ายที่สุด (ผ่านหน้าเว็บ GitHub โดยตรง ไม่ต้องใช้โปรแกรมเสริม)

1. **สมัครและล็อกอินเข้าสู่ GitHub**
   - ไปที่เว็บไซต์ [GitHub.com](https://github.com) แล้วลงทะเบียนใช้งานหรือเข้าสู่ระบบ

2. **สร้างพื้นที่เก็บรหัสข้อมูล (New Repository)**
   - กดปุ่ม **"+"** ที่มุมบนขวา เลือก **New repository**
   - ตั้งชื่อโปรเจกต์ เช่น `chao-phraya-safety-tracker`
   - เลือกสถานะเป็น **Public** (สาธารณะ เพื่อให้ใช้สิทธิ์ GitHub Pages ได้ฟรี)
   - กดปุ่ม **Create repository**

3. **อัปโหลดไฟล์เข้าระบบ GitHub**
   - ในหน้า Repository ใหม่ ให้คลิกลิงก์ **"uploading an existing file"**
   - ลากไฟล์ทั้งหมดในโฟลเดอร์โปรเจกต์ของคุณ (ยกเว้นโฟลเดอร์ `node_modules` และไฟล์ที่ไม่จำเป็น) ไปวางเพื่ออัปโหลด
   - *หรือถ้าต้องการง่ายและเร็วที่สุด*: ลากเฉพาะไฟล์ **`dist/index.html`** ไปอัปโหลด และเปลี่ยนชื่อไฟล์จาก `dist/index.html` ไปวางไว้ที่โฟลเดอร์นอกสุด (Root) ของ Repository โดยใช้ชื่อไฟล์เป็น `index.html`
   - กดปุ่ม **Commit changes** ที่ด้านล่างเพื่อบันทึกไฟล์

4. **เปิดใช้งาน GitHub Pages**
   - ไปที่แถบเมนู **Settings** (รูปฟันเฟือง) ด้านบนของหน้าโปรเจกต์ใน GitHub
   - ที่เมนูด้านซ้าย เลือกหมวดหมู่ **Pages** (ใต้หัวข้อ Code and automation)
   - ภายใต้หัวข้อ **Build and deployment** -> **Source**: ให้เลือกเป็น `Deploy from a branch`
   - ที่หัวข้อ **Branch**: ให้เลือกกิ่งเป็น `main` (หรือ `master`) และเลือกโฟลเดอร์เป็น `/ (root)`
   - กดปุ่ม **Save**

5. **เปิดเข้าใช้งานแอปพลิเคชันจริง**
   - รอระบบ GitHub ทำการอัปโหลดประมาณ 1-2 นาที
   - รีเฟรชหน้าต่างนั้น คุณจะพบลิงก์สีเขียวแสดงที่ด้านบน เช่น:
     `https://<ชื่อผู้ใช้ของคุณ>.github.io/chao-phraya-safety-tracker/`
   - สามารถส่งต่อลิงก์นี้ให้กรรมการตรวจเรือ หรือเจ้าหน้าที่คนอื่นๆ ใช้เปิดในโทรศัพท์มือถือเพื่อเริ่มตรวจสอบเรือได้ทันที!

---

## 📁 การจัดเก็บและสำรองข้อมูล (Data Backup & Reset)
- **ความทนทาน**: ข้อมูลจะคงอยู่ถาวรในเว็บเบราว์เซอร์ของอุปกรณ์นั้นๆ แม้ปิดเครื่องหรือปิดแท็บ
- **ข้อควรระวัง**: ข้อมูลจะหายไปก็ต่อเมื่อมีการ "ล้างประวัติการเข้าชมเว็บและข้อมูลคุกกี้ (Clear Browser Cache / Cookies)" ในมือถือหรือคอมพิวเตอร์เครื่องนั้นๆ เท่านั้น
- **ฟังก์ชันเคลียร์ข้อมูล**: หากต้องการเริ่มต้นใหม่หรือล้างเครื่อง สามารถทำได้โดยไปที่แท็บ **ประวัติบันทึกความปลอดภัย** แล้วกดปุ่มลบประวัติหรือล้างข้อมูลได้ทันที
